{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "php artisan config:cache && php artisan migrate --force && (php-fpm -y /assets/php-fpm.conf &) && nginx -c /assets/nginx.conf -g 'daemon off;'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}